const knex = require('../database/config');

module.exports = {
	add,
	update,
	remove
};

function add(issue) {
	return null;
}

function update(changes, id) {
	return null;
}

function remove(id) {
	return null;
}
